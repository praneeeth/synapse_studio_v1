# ---- Web framework ----
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12       # REQUIRED by FastAPI for UploadFile / multipart forms
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.1

# ---- HTTP client (async, replaces blocking `requests`) ----
httpx>=0.27.0
tenacity>=9.0.0                # retry/backoff around the LLM provider
truststore>=0.10.0             # use the OS trust store (fixes SSL errors behind corporate TLS-inspecting proxies)

# ---- Document parsing ----
pypdf>=5.1.0
python-docx>=1.1.2
markdown-it-py>=3.0.0

# ---- Retrieval stack ----
chromadb>=0.5.20
sentence-transformers>=3.3.0
langchain-text-splitters>=0.3.2
langchain-experimental>=0.3.3
langchain-huggingface>=0.1.2
langchain-community>=0.3.7
numpy>=1.26.0

# ---- Observability ----
structlog>=24.4.0
